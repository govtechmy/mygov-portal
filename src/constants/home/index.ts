import { leftItems, rightItems } from './third_section';
import { features } from './fourth_section';
import { faq } from './faq';

export const data = {
  leftItems,
  rightItems,
  features,
  faq,
} as const;
